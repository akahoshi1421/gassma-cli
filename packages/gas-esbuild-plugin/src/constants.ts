const PLUGIN_NAME = "gassma-gas-esbuild-plugin";

// Prefixed with "__gassma" so it is unlikely to collide with user globals.
const GAS_BUNDLE_GLOBAL_NAME = "__gassmaExports";

export { GAS_BUNDLE_GLOBAL_NAME, PLUGIN_NAME };
